# Deprecated — use TanStack Start routes instead

LiveKit tokens and Monnify payments now live in the frontend app:

- `GET /api/lumen-token`
- `GET /api/payments/config`
- `POST /api/payments/init`
- `GET /api/payments/verify`

Deploy only **frontend** + **agent**. This folder is kept for reference; you do not need to run it.
